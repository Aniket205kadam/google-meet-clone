const AppConfig = {
  googleClientId: String(import.meta.env.VITE_NAMASKAR_GOOGLE_CLIENT_ID),
  backendUrl: String(import.meta.env.VITE_NAMASKAR_BACKEND_URL),
  appRoot: String(import.meta.env.VITE_NAMASKAR_APP_ROOT),
};

export default AppConfig;
